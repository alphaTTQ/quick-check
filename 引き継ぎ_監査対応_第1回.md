# 引き継ぎ: 第1回外部監査(HOLD)への対応指示

作成: 2026-08-20(zaikokanri-saasコア開発セッションより。ユーザー承認済み)
宛先: quick-check実装セッション
監査対象だったコミット: `fe603ffb4cea71c0f4714f6349132dfaeb1a48f6`
監査判定: **HOLD**(指摘5件+プライバシー注意3件)。以下すべて**採用**で確定。

対応完了後は「完了報告」(末尾)を作成して停止すること。
修正コミットを提示して**再監査(ChatGPT)**を受け、PASS後にユーザーのGoでリリース。

---

## 修正1: 医療機関コード欠落時は自動保存・比較を無効化(P0)

- 現状: `hospKey()`がコード欠落時に空文字を返し、別病院が同一キーになる
- 修正: **安定した医療機関キーを作れない場合はキーを生成しない**。
  自動保存・自動比較を行わず、画面に「医療機関コードが読み取れないため
  履歴保存・自動比較の対象外です」と明示する(読取・表示自体は行ってよい)
- パーサ・`recordKeys()`の両方で欠落を弾く(片方だけの防御にしない)

## 修正2: 交付日不明時は新旧判定せず、保存を置換しない(P0)

- 現状: `scannedIsCurrent()`がどちらかの日付欠落で常にtrue → 既存の最新保存を上書き
- 修正: 交付日が取れない場合は**自動新旧判定を行わない**。
  既存保存は**置換しない**。比較表示する場合は
  「交付日不明のため新旧を判定できません(保存分は維持)」を明示する

## 修正3: バックアップ取込の検証と再strip(P0)

- 現状: `bakImport()`が`data.records`を無検証で`Store.putRec()`へ直行 →
  保存最小化(`stripForStorage()`)を迂回し、氏名・生年月日等がそのまま保存される
- 修正: 取込前に**全件の形式・salt・keys・date・savedAtを検証**し、
  `text`を改めて`stripForStorage()`に通してから**原子的に置換**する
  (1件でも不正なら全体を拒否。部分取込をしない)

## 修正4: housekeepingのfail-closed化(P0)

- 現状: スキーマ移行・90日purgeの例外を空catchで破棄(期限切れデータが残っても
  通常利用が続くfail-open)
- 修正: 失敗を`Store.init()`へ返し、**保存・自動比較を無効化して利用者へ通知**する
  (読取・その場表示は許可してよい。「消せていないのに溜め続ける」だけを止める)

## 修正5: カナ⇔漢字の照合 — 候補提示+手動確認方式(ユーザー決定済み)

- 現状: カナのみ保存→漢字のみ読取(またはその逆)で同一患者を照合できず、
  黙って「初回」になる
- 修正(決定): 氏名キーが不一致でも**「生年月日+性別+医療機関+診療科」が一致する
  保存分があれば、自動比較はせず**、
  「氏名表記が異なる保存分があります。同一患者なら選択して比較」と
  **候補提示+手動確認**にする。確認されたら両方の氏名キーを統合し、
  以後は自動照合されるようにする(分裂していた既存レコードもこの経路で統合される)
- 候補が複数ある場合も一覧提示でよい。**自動で1件に決めない**

## 修正6: 自由記述レコードの保存除外(ユーザー決定済み)

- 現状: レコード81・181・281等の自由記述が全文保存され、患者名等が
  紛れ込む経路が残る
- 修正(決定): **自由記述は保存対象から除外**する(読取時のその場表示のみ)。
  比較の本質(薬剤の同一性)には不要のため

## 修正7: バックアップの取扱表示と既知の限界の明記

- バックアップにはsaltが同梱され、辞書照合を防げない。**匿名データとして
  案内しない** — 「処方内容と疑似識別子を含む要保護データ」として、
  書き出し時の画面文言・ファイル名・README相当の説明を修正する
- 「同一医療機関・診療科内で氏名・生年月日・性別がすべて同じ別人は
  区別できない」を既知の限界として使い方説明に明記する

---

## 回帰テストの追加(HOLD解除条件)

修正1〜6それぞれに、監査で再現された経路のテストを足すこと:

1. 機関コード空×2病院 → 同一キーにならない(保存・比較が無効化される)
2. 交付日空 → 既存保存が置換されない/新旧判定が出ない
3. 不正・旧形式レコードを含むバックアップ取込 → 全体拒否。
   正常バックアップ → strip済みの内容だけが保存される
4. purge失敗(モック) → 保存・自動比較が無効化される
5. カナのみ保存→漢字のみ読取 → 「初回」と断定せず候補提示。
   確認後はキー統合され自動照合される(逆方向も)
6. 自由記述入りQR → 保存内容に自由記述が含まれない

※ 監査環境で`test2_e2e.js`がiconv不在で未実行だった件: 依存の同梱または
   スキップ時の明示(SKIPPED表示)を検討(監査者が「未確認」を抱えないように)。

## 完了報告(作成して停止)

1. 修正コミットhash
2. 修正1〜7それぞれの対応内容(ファイル・関数名)
3. 追加テスト一覧と全テスト実行結果
4. 設計上の判断が必要になった点(あれば)
5. 監査者への再監査依頼文(対象コミット明示)

---

## 連絡事項

- 指摘1・2は、元の引き継ぎ(引き継ぎ_UI改修_履歴保存.md)の仕様漏れが原因。
  設計側(コアセッション)の責任であり、そちらの実装判断の誤りではない
- 体制はこれまでどおり: 実装=そちら / 監査=ChatGPT1回(再監査) / 査読なし。
  リリースの最終判断はユーザーのGo
