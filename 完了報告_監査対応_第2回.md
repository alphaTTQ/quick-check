# 完了報告: 第2回外部監査（HOLD）対応

作成: 2026-08-21（quick-check実装セッション）
前回監査対象: `f41d034f604d4e69fee71dab255140820959e651`（判定HOLD・解除条件6項目）

## 1. 修正コミット

**`37d7179b439bcb6c13268be588663bb90a7fc0f6`**

変更ファイル: `src/app_src.html`・`index.html`（ビルド生成物）・
`test/test6_master.js`（改行耐性）・`test/test9_audit.js`（idを正規形式に）・
`test/test10_audit2.js`（新規）・`.gitattributes`（新規）・
`完了報告_監査対応_第1回.md`（変更範囲の訂正）・本文書。

## 2. 解除条件への対応

### 条件1: 保存領域の実エラーのreject化とreadyガード

- `txDone(tx)` を新設し、**全ての更新系トランザクション**（`metaSet`・`setSalt`・
  `getSalt`の書き戻し・`putRec`・`delRec`・`clear`・`replaceAll`）で
  `onerror`・`onabort` を必ずrejectするよう統一
- `flush()` のlocalStorage書込み例外を握りつぶさず例外化（呼び出し元の
  putRec等が失敗として伝搬し、housekeeping経由なら `Store.degraded` になる）
- `Store.ready()`（initのPromiseを保持）を新設し、`processScanned`・`bakImport`・
  `exportData` は **init完了を待ってから**実行される
- 回帰テスト（test10）: フェイクIndexedDBで**書き込みトランザクションのabort**を
  実発火させ degraded→保存/比較停止→通知 を検証。localStorageは**容量超過相当の
  実書込み例外**で同様に検証し、復旧経路も確認

### 条件2: バックアップの相互整合性検証

`validateBackup` を強化（すべて実装済み・test10で検証）:

- `id`: 64桁16進のみ（監査で実証された `');alert(...)//` 形式は形式検査で拒否）
- `date`: `strictDateKey` による実在日付検証（`20261399` は拒否）
- `savedAt`: 未来値（現在+60秒超）を拒否（purgeの無期限回避を遮断）
- `keys`: レコード横断で重複を拒否（`findByKeys` 先勝ちによる誤比較の原因を遮断）
- `text`: strip後にJAHISとして解析し、**解析可能かつ交付日がdateと一致**することを検証
- 保存期限切れレコードは取込から除外（件数を利用者に表示）

### 条件3: インラインイベント経由のスクリプト注入の除去

- 候補確認ボタンの `onclick="confirmCandidate('${id}')"` を全廃。
  `data-cand-idx`（配列添字）＋`#result`への委譲リスナー方式に変更し、
  **保存レコードのidをHTML属性に埋め込まない**（test10で `onclick` 不在と
  id非出現をHTML出力に対して検証）

### 条件4: 交付日の厳格検証

- `strictDateKey()` 新設: 西暦8桁・和暦7桁の**実在する暦日のみ**（月日の実在・
  閏年・年範囲1926〜2100）をキー化。通常読取（`processScanned`）・保存
  （`saveCurrent`）・候補確認・バックアップ検証のすべてで使用
- `20269999`・2/31・非閏年の2/29等は「交付日不明」となり、**保存されない**
  （汚染の入口を遮断。表示用の `parseJDate` は従来どおりで、画面表示のみに使用）

### 条件5: クリーン展開での全テスト再実行

コミット `37d7179` を `git clone --no-local` で別ディレクトリに展開して実行:

| スイート | 結果 |
|---|---|
| test1_sa（分割QR） | 9 PASS |
| test2_e2e（QR生成→光学→Shift-JIS） | 8 PASS（iconvあり環境で実行） |
| test3_diff（突合） | 37 PASS |
| test4_store（照合キー） | 25 PASS |
| test5_strip（保存最小化） | 47 PASS |
| test6_master（コード表・**改行耐性修正済み**） | 11 PASS |
| test7_flow（フロー統合） | 20 PASS |
| test8_logic（判定純関数） | 21 PASS |
| test9_audit（第1回指摘再現） | 36 PASS |
| test10_audit2（第2回指摘再現・新規） | 33 PASS |
| **合計** | **247 PASS / 0 FAIL / 0 SKIP** |

test6は改行正規表現を `;\r?\n` に修正し、さらに `.gitattributes`（`* -text`）で
行末変換自体を停止したため、クローン環境のautocrlf設定に依存しない。
test2は監査環境にiconvが無い場合 `SKIPPED` を明示して正常終了する（前回対応済み）。

### 条件6: 完了報告の変更範囲の訂正

`完了報告_監査対応_第1回.md` の変更範囲記載を実際の差分（README.md・index.html・
受領文書2件を含む）に訂正済み。

## 3. 設計上の判断

1. readyガードは「拒否」ではなく「**init完了までの待機**」とした。アプリは
   読み込み時に必ず `Store.init()` を起動するため、完了前の読取は待機後に
   正常処理される（test10で、initが遅延する環境でも保存まで到達することを検証）。
   initが呼ばれない異常系はrejectされ、保存・比較は行われない。
2. 期限切れバックアップレコードは「全体拒否」ではなく「除外＋件数表示」とした。
   次回起動のpurgeで消えるだけのデータであり、正当な移行を妨げないため。
   除外が発生したことは取込確認ダイアログと完了メッセージの両方に表示する。
3. `strictDateKey` の年範囲は1926〜2100（昭和以降）。JAHIS規約の和暦年号区分
   （明治〜令和）より狭いが、処方せん交付日として1926年以前・2100年以降は
   実務上あり得ず、範囲外は「不明」に倒す方が安全と判断した。

## 4. 再監査依頼文（そのままChatGPTへ）

---

あなたは薬局向けWebツール「quick-check」の外部監査者です。第2回監査（対象
`f41d034f…`、判定HOLD、解除条件6項目）への修正を再監査してください。

- リポジトリ: https://github.com/alphaTTQ/quick-check （公開）
- **再監査対象コミット: `37d7179b439bcb6c13268be588663bb90a7fc0f6`**
- 対応内容: リポジトリ内 `完了報告_監査対応_第2回.md`（本文書）

確認事項:
1. 解除条件6項目がそれぞれ実物コードで満たされているか
   （`txDone`／`flush`／`Store.ready`／`validateBackup`／委譲リスナー／
   `strictDateKey`／test6・.gitattributes／完了報告の訂正）
2. 修正が新たな欠陥を作っていないか（特に: readyガードの待機方式の妥当性、
   期限切れバックアップの除外仕様、strictDateKeyの和暦変換）
3. `test/test10_audit2.js` のフェイクIndexedDB・localStorageが指摘の
   実障害経路を実際に検証できているか

判定形式は従来どおり: CONFIRMED / MISMATCH / UNVERIFIED、総合判定 GO / HOLD。
実物のコード行を根拠として引用すること。

---

以上。再監査PASS後、ユーザーのGoでリリース。
