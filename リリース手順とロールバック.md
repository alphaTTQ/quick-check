# リリース手順とロールバック

## 配布・デプロイ先

| 項目 | 内容 |
|---|---|
| 公開URL | https://alphattq.github.io/quick-check/ |
| 配信方式 | GitHub Pages（`main` ブランチのルート `index.html` を静的配信） |
| リポジトリ | https://github.com/alphaTTQ/quick-check （公開） |
| 現行リリース | **v1.0.0** = `cd573a261534e07d2b349c100d956c50b22624c5` |
| Release | https://github.com/alphaTTQ/quick-check/releases/tag/v1.0.0 |

**注意: 公開URL（オリジン）を変えると全端末の保存データが失われる。**
保存は `https://alphattq.github.io` オリジンの IndexedDB にあるため、
リポジトリ名・配信先の変更は移行手順とセットでしか行わないこと。

## 通常のリリース手順

```bash
cd "C:/Users/keika/Desktop/作業用フォルダ/quick-check"
perl src/build.pl                                   # src/ から index.html を生成
cd test && for t in test*.js; do node "$t" || break; done   # 全スイート実行
cd .. && git add -A && git commit && git push        # push の数分後に Pages へ反映
```

反映確認:
```bash
curl -s https://alphattq.github.io/quick-check/ | sha256sum
sha256sum index.html      # 一致すれば反映完了
```

## ロールバック

### 方法1: 直前の版に戻す（推奨・履歴が残る）

```bash
cd "C:/Users/keika/Desktop/作業用フォルダ/quick-check"
git revert --no-edit <戻したいコミット>
git push
```
数分後に Pages へ反映される。`git reset --hard` + force push は履歴が壊れ、
監査対象コミットの参照も切れるため使わない。

### 方法2: 特定バージョンの配信物へ即時差し替え

```bash
git checkout v1.0.0 -- index.html      # タグ指定で配信物だけ差し戻す
git commit -m "index.html を v1.0.0 に差し戻し" && git push
```

### 方法3: ローカル保管の配信物から復旧（リポジトリが使えない場合）

`C:\Users\keika\Desktop\作業用フォルダ\処方せん監査\旧版保管\` に配信物を保管:

| ファイル | 版 | SHA-256 |
|---|---|---|
| `index_v1.0.0_cd573a2.html` | v1.0.0（現行） | `b2e0c8c4ac5a73ff59f90ed94a6dd9c4116de1e4bb51eaa6477cd48d7eda331a` |
| `index_prev_37d7179.html` | 第2回監査対応時点 | `b2ac1b9abccedea60a629b02aa1041abae8a7c980ec75952e399381693f4f2ab` |

これを `index.html` としてリポジトリに置いて push する。
またローカルの `作業用フォルダ\処方せん監査\処方せん監査.html` も同一内容であり、
PC単体での応急運用（`カメラで使う.bat` 経由）に使える。

### 旧版の保管場所（まとめ）

- **Git**: 全バージョンが `main` の履歴に保管。タグ `v1.0.0` で参照可能
- **GitHub Release**: v1.0.0 のソースZIP（GitHubが自動生成）
- **ローカル**: 上表の `旧版保管\` フォルダ

## ロールバック後に必ず確認すること

保存データのスキーマ（`Store` の `SCHEMA` 定数）が変わった版から戻す場合、
**旧版は新スキーマのデータを認識せず、起動時に全消去する**（意図した動作）。
スキーマを変更したリリースを戻すときは、事前に利用者へ
「保存されている前回処方が消える」ことを伝えること。
v1.0.0 のスキーマは `3`。
