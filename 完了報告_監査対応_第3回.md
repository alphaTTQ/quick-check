# 完了報告: 第3回外部監査（HOLD）対応

作成: 2026-08-21（quick-check実装セッション）
前回監査対象: `37d7179b439bcb6c13268be588663bb90a7fc0f6`（判定HOLD・解除条件5項目）

## 1. 修正コミット

**`d9b5a4c44aca00152662e63ab500bc3fa0f807c4`**

変更ファイル: `src/app_src.html`・`index.html`（ビルド生成物）・
`test/test11_audit3.js`（新規）・本文書。

## 2. 解除条件への対応

### 条件1・2: localStorage更新の copy-on-write 化と、実行中失敗の degraded 移行

前回の修正は `flush()` で例外を投げるだけで、**先に `mem` を書き換えてから永続化**
していた。指摘のとおり、これは「保存に失敗したのにメモリ上は成功扱い」という
前回より悪い状態であり、未保存レコードが次回比較に使われる quietly wrong だった。

- `flush()` を廃止し **`commit(next)`** を新設。変更後の状態 `next` を先に
  `localStorage.setItem` し、**成功した場合にのみ `mem` を差し替える**。
  失敗時は `mem` を一切変更せず、`degraded` を設定して例外を投げる
- 対象は全ての更新経路: `putRec` / `delRec` / `clear` / `setSalt` / `getSalt`
  （塩の新規生成）/ `metaSet` / `replaceAll`
- `txDone`（IndexedDB）も abort/error 時に `degraded` を設定するよう変更。
  これで **localStorage・IndexedDB どちらも、起動時だけでなく実行中の
  書き込み失敗で degraded** になり、以後の保存・自動比較が停止する

### 条件3: degraded 中のバックアップ取込の拒否

- `bakImport` に `Store.degraded` の確認を追加し、理由を添えて拒否する
  （容量制限が解除されても、再初期化するまでは取り込めない）
- 書き出し（`exportData`）は読み取りのみのため許可のまま

### 条件4: 和暦の元号年・開始日・終了日の検証

- `ERA` に `from` / `to`（元号の実期間・YYYYMMDD）を追加
- `strictDateKey` で **元号年 ≥ 1**（`00` を拒否）を確認し、変換後の西暦日付が
  **元号期間内**であることまで検証する

指摘された5例はすべて拒否されることを確認（境界の内側は通ることも併せて検証）:

| 入力 | 結果 |
|---|---|
| 昭和元年1月1日 `3010101` | 拒否（昭和は12/25から） |
| 平成元年1月1日 `4010101` | 拒否（平成は1/8から） |
| 令和元年1月1日 `5010101` | 拒否（令和は5/1から） |
| 昭和64年1月8日 `3640108` | 拒否（昭和は1/7まで） |
| 平成31年5月1日 `4310501` | 拒否（平成は4/30まで） |
| 元号年00 `3000101` `5000501` | 拒否 |
| 昭和64年1月7日 `3640107` | **通る**（昭和最終日） |
| 平成31年4月30日 `4310430` | **通る**（平成最終日） |
| 令和元年5月1日 `5010501` | **通る**（令和初日） |

### 条件5: 回帰試験の追加と全試験結果

新規 `test/test11_audit3.js`（37件）:

- 元号境界13件（上表の全ケース＋明治・大正の年範囲外拒否）
- copy-on-write: 保存失敗後もメモリが書き換わらない／実行中失敗で degraded ／
  degraded 中は保存・自動比較しない／復旧後は正常に比較される（7件）
- **各更新操作の失敗時ロールバック**: `putRec`・`delRec`・`clear`・`setSalt`・
  `replaceAll` それぞれで例外が投げられ、レコード・塩が変化しないこと（7件）
- degraded 中のバックアップ取込拒否と、再初期化後の取込成功（5件）
- IndexedDB の**実行中** abort で degraded になり以後停止すること（4件）

クリーン展開（`git clone --no-local` → 対象コミットを checkout）での実行結果:

| スイート | 結果 |
|---|---|
| test1_sa | 9 PASS |
| test2_e2e | 8 PASS |
| test3_diff | 37 PASS |
| test4_store | 25 PASS |
| test5_strip | 47 PASS |
| test6_master | 11 PASS |
| test7_flow | 20 PASS |
| test8_logic | 21 PASS |
| test9_audit | 36 PASS |
| test10_audit2 | 33 PASS |
| test11_audit3（新規） | 37 PASS |
| **合計** | **284 PASS / 0 FAIL / 0 SKIP** |

あわせて、クリーン展開で `perl src/build.pl` を実行して生成した `index.html` が
**コミット済みの `index.html` とバイト単位で一致**することを確認済み。

## 3. 設計上の判断

1. degraded からの復旧は「再初期化（ページの開き直し）」に限定した。書き込みが
   成功するたびに自動解除する案もあるが、一度でも整合性が崩れた可能性のある
   状態で保存を再開するより、明示的な再初期化で housekeeping を通し直す方が安全。
   画面には「ページを開き直してください」と表示する。
2. `commit(next)` は `mem` の浅いコピー＋`recs` の浅いコピーを作る。レコード自体は
   不変オブジェクトとして扱う（更新時は常に新しいレコードを `putRec` する）ため、
   深いコピーは不要と判断した。
3. 明治・大正は `ERA` に期間を定義したが、`strictDateKey` の年範囲（1926〜2100）で
   先に弾かれる。処方せんの交付日として1926年以前は実務上あり得ないため。
   表示用の `parseJDate` は従来どおり明治・大正も解釈する（生年月日の表示に使う）。

## 4. 再監査依頼文（そのままChatGPTへ）

---

あなたは薬局向けWebツール「quick-check」の外部監査者です。第3回監査（対象
`37d7179b…`、判定HOLD、解除条件5項目）への修正を再監査してください。

- リポジトリ: https://github.com/alphaTTQ/quick-check （公開）
- **再監査対象コミット: `d9b5a4c44aca00152662e63ab500bc3fa0f807c4`**
- 対応内容: リポジトリ内 `完了報告_監査対応_第3回.md`（本文書）

確認事項:
1. 解除条件5項目が実物コードで満たされているか
   （`commit()` による copy-on-write／実行中失敗の degraded 移行／
   degraded 中の取込拒否／`ERA` の from-to と元号年検証／test11 と全試験結果）
2. 修正が新たな欠陥を作っていないか（特に: degraded の解除条件を
   再初期化のみとした判断、`commit` の浅いコピーで十分か、
   `getSalt` の塩生成失敗時の挙動）
3. `test/test11_audit3.js` が指摘の実障害経路を実際に検証できているか

判定形式は従来どおり: CONFIRMED / MISMATCH / UNVERIFIED、総合判定 GO / HOLD。
実物のコード行を根拠として引用すること。

---

以上。再監査PASS後、ユーザーのGoでリリース。
