# 監査依頼: quick-check 取り込み1本化・履歴保存モデル変更

（この文書をそのままChatGPTに貼り付けて監査を依頼する。リポジトリは公開のため
監査者は実物コードを直接参照できる）

---

あなたは薬局向けWebツール「quick-check」の外部監査者です。以下の変更について、
実物コードを Source of Truth として監査してください。

## 対象

- リポジトリ: https://github.com/alphaTTQ/quick-check （公開）
- **対象コミット: fe603ffb4cea71c0f4714f6349132dfaeb1a48f6**
- 主対象: `src/app_src.html`（アプリ本体。`index.html` はビルド生成物）
  - 比較ロジック純関数: `scannedIsCurrent` / `decideScan` / `purgeDue` / `chooseRecord`
  - 照合鍵: `recordKeys` / `hospKey` / `deptKey`（患者×医療機関×診療科、SHA-256）
  - フロー: `processScanned`（読取→初回/比較の分岐→保存）
  - 保存層: `Store`（IndexedDB、`housekeeping` でスキーマ移行と90日purge）
  - 保存内容の最小化: `stripForStorage` / `STORE_KEEP`
- テスト: `test/test4_store.js`（鍵）/ `test7_flow.js`（フロー統合）/ `test8_logic.js`（判定表）

## 背景（変更の意図）

処方せんQRを読み、同一キー（患者×医療機関×診療科）の保存済み前回処方と自動比較する。
患者は複数医療機関の処方せんを持ち込むため、別機関・別診療科を誤って比較しない
ことが最重要。取り込みメニューは1本で、前回/今回は交付日で自動判定する。

## 監査論点（この2点に絞る）

### 1. 比較ロジックの正しさ

「quietly wrong」——誤った比較が正しい顔をして表示される欠陥——が無いか。特に:

- 別医療機関・別診療科・診療科記載なしの処方が、同一キーとして照合される経路は無いか
  （`recordKeys` のスコープ構成、`deptKey` の空文字の扱い、ハッシュ入力の区切り文字）
- 交付日の新旧判定と保存の置き換え判定が食い違う経路は無いか
  （`decideScan` と `chooseRecord` の整合。画面で「今回」扱いなのに保存されない、逆は無いか）
- 「初回（比較対象なし）」であるべきものが比較表示される、またはその逆は無いか
- 同姓同名・生年月日同一の別人が衝突した場合の挙動は説明可能か（既知の限界として許容可か）
- カナのみ/漢字のみ記録の医療機関間で、同一患者の照合が破綻する経路は無いか
  （`keys` 配列のマージ運用を含む）

### 2. 患者情報の保存の妥当性

- 保存データ（IndexedDB）に患者氏名・カナ・生年月日・保険情報・引換番号が
  残る経路は無いか（`stripForStorage` の網羅性、`saveCurrent` が必ずそれを通すか）
- 照合鍵から個人を復元できないか（塩の管理、書き出し時の塩の同梱の妥当性）
- 90日purge・スキーマ移行時の全消去・手動全消去が仕様どおり安全側に動くか
  （`purgeDue` の境界、`housekeeping` の失敗時挙動）
- 書き出し（バックアップ）データの内容は保存データと同じ最小化がされているか

## 判定形式

指摘ごとに CONFIRMED（実物で確認した欠陥）/ MISMATCH（報告と実物の食い違い）/
UNVERIFIED（確認できず）を付け、最後に総合判定 GO / HOLD を出してください。
実物のコード行を根拠として引用すること。憶測での指摘は UNVERIFIED として区別すること。
