# 引き継ぎ: 取り込みメニュー1本化と履歴保存モデルの変更

作成: 2026-08-20(zaikokanri-saasコア開発セッションより)
宛先: quick-check実装セッション
※ ユーザーと合意済みの設計。実装前に疑問があればユーザーへ確認を。

## 目的

1. トップ画面の取り込みメニューを**1種類に統一**する(「前回用/今回用」を人が選ばない)
2. 履歴の保存を**患者×医療機関×診療科ごとに直近1回のみ**へ変更する
   (別医療機関・別診療科の処方せんを誤って比較しない)

## 背景(ユーザーの気づき)

- 患者は複数の医療機関の処方せんを持ってくる。意味のある比較は
  **同じ医療機関(かつ同じ診療科)の前回処方との比較**だけ
- 総合病院では内科と整形外科など、同一機関でも診療科ごとに別の処方が並行する
- QRには交付日が入っているので、2枚読めばどちらが新しいかはアプリが判断できる

## 保存モデル

- **キー**: 患者識別(氏名+生年月日) × 医療機関コード × 診療科
  - すべてQRから取得(手入力なし)。医療機関コードは都道府県+点数表+機関コード
  - **診療科レコードが無いQRは「診療科なし」という1つのキー値として扱う**
    (欠落を理由に機関単位へ黙って合流させない/エラーにもしない)
  - ※ 診療科がJAHIS規約上どのレコードで来るか・実QRでの記載率は、
    規約とデコード実績を持つそちらのセッションで確定してほしい。
    もし実QRでの記載率が低いようなら、その事実をユーザーへ報告のうえ
    機関単位キーへの単純化を再提案してよい
- **値**: 直近1回の処方内容のみ(履歴の蓄積はしない)
- **保存期間: 90日で自動削除**(起動時にpurge)。設定に「履歴の全消去」ボタンを置く
  - 理由: 端末に患者の処方情報が残り続ける面を最小化する(オフライン設計は維持)

## 取り込みフロー(メニュー1本)

```
「処方せんを読む」(1メニューのみ)
  1. QR解析 → 患者・医療機関・診療科・交付日・処方内容を抽出
  2. 同一キーの保存分を検索
     - 無し → 「初回(比較対象なし)」と明示表示 → 今回分を保存
     - 有り → 交付日で新旧を判定して比較表示:
         読んだ方が新しい → 保存分=前回 / 読んだ方=今回 として比較 → 保存分を置き換え
         読んだ方が古い   → 読んだ方=前回 / 保存分=今回 として比較 → 保存分は置き換えない
           (古い紙を後から読んでも、保存されている最新を上書きしない)
         同日 → 読取時刻の新しい方を「今回」とする
  3. 比較画面には「比較対象: ○○病院 △△科 YYYY-MM-DD交付分」を必ず表示
```

## UI上の必須事項

- **「初回(比較対象なし)」と「比較済み」を画面上はっきり区別する**。
  別キーの処方を黙って比較して差分だらけにするのが最も危険(警告疲れの温床)
- 比較対象のキー(医療機関名・診療科・交付日)を比較結果に明示する
- 90日purge・全消去はユーザー操作なしで安全側に動くこと

## やらないこと(今回のスコープ外)

- 診療科より細かい分別(処方医単位等) — 実運用で潰れが出てから検討
- 履歴の複数世代保持 — 直近1回で足りる。必要になったら再設計
- 端末間の履歴同期 — オフライン設計(CSP遮断)を維持する

## 完了条件の提案

1. メニュー1本で上記フローが動く(新→旧、旧→新、同日、初回の4パターン)
2. キー違い(別機関・別診療科)は比較されず「初回」になる
3. 90日purgeと全消去が動く
4. 診療科レコードの実QRでの取得可否の確認結果を報告
5. **比較ロジックをテスト可能な純関数に切り出し、テストを付ける**
   (キー一致判定・交付日の新旧判定・初回判定・90日purge対象判定。
   「この2枚のQRならこの判定」をテストで固定する。UI・localStorageから
   切り離した関数にすれば単一HTML構成のままでもnode:test等で検証できる。
   zaikokanri-saas側の台帳エンジン(src/lib/ledger/)と同じ流儀)

## リリース前の外部監査(ユーザーと合意済みの体制)

実装完了後、リリース前に**ChatGPTによる監査を1回**受ける。論点は2つに絞る:

1. 比較ロジックの正しさ(キー照合・新旧判定・初回明示 — 別診療科の処方を
   静かに比較してしまう類の「quietly wrong」が無いか)
2. 患者情報の保存の妥当性(保存内容の最小化・90日purge・全消去)

quick-checkは公開リポジトリなので、監査者は実物コードを直接参照できる
(コミットhashを伝えればよい)。査読セッション(第3役)は不要
(DB・本番適用が無く、報告と実体の照合は監査者自身が実物で行えるため)。

## 連絡事項(マスタ側は完了済み)

- export:quickcheck の出力(data/drug-master-raw.js + manifest)は生成済み・取り込み済み
- YJ未結合821件は名称つき全量を薬品コード表/20260820/YJ未結合リスト.txt に保存済み
  (将来の処方⇔現物変換の対象リスト)
- マスタは月次のGS1更新サイクルで逐次更新される(コアで1回更新→export:quickcheck→
  そちらでビルド1回)
