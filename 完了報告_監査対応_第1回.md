# 完了報告: 第1回外部監査（HOLD）対応

作成: 2026-08-21（quick-check実装セッション）
対応指示: `引き継ぎ_監査対応_第1回.md`（全指摘採用）

## 1. 修正コミット

**`f41d034f604d4e69fee71dab255140820959e651`**
（監査対象だった fe603ff からの差分がすべての修正。src/app_src.html と test/ のみ、
DBや外部サービスへの変更なし）

## 2. 修正内容（ファイルはすべて src/app_src.html）

| # | 対応 | 実装箇所 |
|---|---|---|
| 修正1 | 医療機関コード欠落時は照合キーを生成しない（`recordKeys`・`candidateKey` の両方で遮断）。保存・自動比較を行わず「医療機関コードが読み取れないため履歴保存・自動比較の対象外です」を表示。読取・表示は継続 | `recordKeys` / `candidateKey` / `processScanned` |
| 修正2 | `scannedIsCurrent` を「両方の交付日がある場合のみ新旧を確定」に変更。`decideScan` が `dateUnknown` を返し、保存は置換しない。初回でも交付日不明なら保存しない（`chooseRecord` が keep）。画面に「交付日が読み取れないため新旧を判定できません（保存分は維持）」を表示 | `scannedIsCurrent` / `decideScan` / `chooseRecord` / `showComparison` |
| 修正3 | `validateBackup` で全件検証（形式・salt・keys(64hex)・date(8桁必須)・savedAt・cand・id重複）。1件でも不正なら全体拒否。`text` は改めて `stripForStorage` に通し、`Store.replaceAll`（IndexedDBは単一トランザクション）で原子的に置換 | `validateBackup` / `bakImport` / `Store.replaceAll` |
| 修正4 | `housekeeping` の例外を握りつぶさず `Store.init` へ伝搬。失敗時は `Store.degraded` を立て、保存・自動比較を停止して画面（保存バー＋メッセージ）に通知。読取・その場表示は継続 | `Store.init` / `housekeeping` / `processScanned` / `renderStoreInfo` |
| 修正5 | 氏名を含まない候補キー（生年月日+性別+医療機関+診療科のハッシュ、`cand`）を各保存に付与。氏名キー不一致でも候補があれば自動比較せず一覧提示（複数可・自動確定しない）。「同一患者として比較」で照合キーを統合し以後は自動照合、「別人として初回扱いで保存」で独立レコード化 | `candidateKey` / `processScanned` / `confirmCandidate` / `rejectCandidates` / `renderResult`（candidates分岐） |
| 修正6 | 自由記述レコード（備考81・用法補足181・薬品補足281）を `STORE_KEEP` から除外。保存分との比較では指示の差分を比較対象外にし（`diffPair` の skipSupp）、「指示・備考は保存対象外のため比較していません」を表示 | `STORE_KEEP` / `diffPair` / `compare` / `showComparison` |
| 修正7 | 書き出しダイアログを「要保護データ」表記に変更（匿名データと案内しない）。同一機関・診療科内の同姓同名・同生年月日・同性別は区別できない旨を README と「はじめにお読みください.txt」の判定の限界に明記 | `exportData` / README.md / 説明書 |

付随: 保存スキーマを v3 に更新（cand追加・交付日必須）。移行時に旧保存は全消去
（リリース前のため実害なし）。

## 3. 追加テストと実行結果

新規 `test/test9_audit.js`（36件）— 監査で再現された経路をそのまま再現:

- 修正1: コード欠落×2病院が合流しない・保存されない・理由表示（6件）
- 修正2: 交付日不明で置換されない・新旧判定が出ない・初回でも保存しない（6件）
- 修正3: 氏名入りテキストの再strip／不正1件で全体拒否／旧形式（交付日なし）拒否（4件）
- 修正4: housekeeping失敗（テストフック）→ degraded → 保存・比較停止 → 復旧（5件）
- 修正5: カナ→漢字・漢字→カナ両方向の候補提示、確認後の統合・自動照合、別人分岐（9件）
- 修正6: 自由記述が保存に残らない・指示差分を誤検出しない・明示表示（6件）

既存テストの更新: test4（機関必須化・chooseRecordの交付日keep）、test5（81/181/281を
除外側へ）、test8（日付不明セマンティクス。不変条件は「置換予告⇔保存の実際が全25通りで
一致」に改訂）。test2 は iconv 不在時に SKIPPED を明示して正常終了。

**全9スイート PASS**（test1_sa 9 / test2_e2e 8 / test3_diff 37 / test4_store 25 /
test5_strip 47 / test6_master 11 / test7_flow 20 / test8_logic 21 / test9_audit 36 = 214件）。

## 4. 設計上の判断（実装時に確定させた点）

1. **交付日不明の初回は「保存しない」**とした。指示は「既存保存を置換しない」のみだが、
   日付なしレコードの保存を許すと以後の新旧判定が永久に不能になる罠があるため、
   入口で塞いだ（JAHIS規約上、交付年月日レコード51は必須出力であり、欠落は異常データ）。
2. **保存分との比較では指示（補足）の差分を比較しない**。修正6で保存から自由記述を
   除外した結果、保存側の指示は常に空になり、素朴に比較すると「指示追加」を毎回
   誤検出する。比較スキップ＋その旨の表示で「quietly wrong」の置き換わりを防いだ。
3. **候補キーの衝突**（同生年月日・同性別・同機関・同診療科の別人）は候補提示に
   出るだけで、自動では何も起きない。選択画面に薬品リストを出し、判断材料とした。
4. テスト用に `Store._test.failHousekeeping` フックを追加（本番コードからは常にfalse。
   housekeeping失敗を外部から再現する手段が他にないため）。

## 5. 再監査依頼文（そのままChatGPTへ）

---

あなたは薬局向けWebツール「quick-check」の外部監査者です。第1回監査（対象
`fe603ffb…`、判定HOLD、指摘5件＋プライバシー注意3件）への修正を再監査してください。

- リポジトリ: https://github.com/alphaTTQ/quick-check （公開）
- **再監査対象コミット: `f41d034f604d4e69fee71dab255140820959e651`**
- 対応内容の一覧: リポジトリ内 `完了報告_監査対応_第1回.md`（本文書）
- 修正指示: `引き継ぎ_監査対応_第1回.md`

確認事項:
1. 第1回指摘（修正1〜7）がそれぞれ実物コードで塞がれているか
2. 修正が新たな「quietly wrong」を作っていないか（特に: 候補提示フローでの
   キー統合の妥当性、交付日不明時の表示と保存の整合、比較対象外となった
   指示差分の扱い）
3. `test/test9_audit.js` が指摘の再現経路を実際に検証できているか

判定形式は前回同様: 指摘ごとに CONFIRMED / MISMATCH / UNVERIFIED、
総合判定 GO / HOLD。実物のコード行を根拠として引用すること。

---

以上。**再監査PASS後、ユーザーのGoでリリース**（現状はPagesに配信済みだが
リリース前扱いのまま）。
